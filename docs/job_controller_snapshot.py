# Snapshot of controllers/job_controller.py (results + editor + browse)
# Used for reference. See IMPLEMENTATION.md for route list and flow.
# Key: _resolve_output_and_pdf, _job_or_folder_context, GET /api/results/{id}/editor, GET /results/{id}, GET /browse
